# SentioCap

Scaffolded full-stack project structure for:
- Next.js 14 frontend (`frontend`)
- FastAPI backend (`api`)
- Supabase configuration (`supabase`)

See `build-spec/project-structure.md` for the authoritative structure and build guidance.
